import XCTest
@testable import CrossReduxSOA

final class CrossReduxSOATests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(CrossReduxSOA().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
